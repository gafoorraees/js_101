// Will the code below raise an error?

let numbers = [1, 2, 3];
numbers[6] = 5;

// No. The spaces for arrays 3, 4, and 5 will be empty.

// Bonus:

let numbers = [1, 2, 3]; 
numbers[6] = 5;
numbers[4]; // What will this line return? Undefined with an empty slot.
